import React from "react"
import { render, screen } from "@testing-library/react"
import { IndexItem } from "."

describe("IndexItem", () => {
  it("renders", () => {
    render(<IndexItem />)
  })
})
