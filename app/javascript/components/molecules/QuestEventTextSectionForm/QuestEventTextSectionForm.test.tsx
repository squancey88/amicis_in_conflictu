import React from "react"
import { render, screen } from "@testing-library/react"
import QuestEventTextSection from "."

describe("QuestEventTextSection", () => {
  it("renders", () => {
    render(<QuestEventTextSection />)
  })
})
