<%= form_with(model: gaming_group) do |form| %>
  <% if gaming_group.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gaming_group.errors.count, "error") %> prohibited this gaming_group from being saved:</h2>

      <ul>
        <% gaming_group.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
