<%= form.text_field(:name) %>

<%= render JsonEditorComponent.new(model: @character_type,
                              attribute: :config,
                              schema: Character.character_config_schema) %>
