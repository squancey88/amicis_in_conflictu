<%= form_with(model: @card_game) do |form| %>

  <%= render RecordFormWrapperComponent.new(record: @card_game, form:) do |component| %>

    <% component.with_form_box do %>
      <%= render partial: "game_systems/base_fields", locals: {form:, game_system: @card_game} %>
    <% end %>
  <% end %>
<% end %>