<%= form_with(model: game_system) do |form| %>

  <%= render RecordFormWrapperComponent.new(record: game_system, form:) do |component| %>

    <% component.with_fields do %>

      <div>
        <%= form.label :name, style: "display: block" %>
        <%= form.text_field :name %>
      </div>
      
    <% end %>
  <% end %>
<% end %>
