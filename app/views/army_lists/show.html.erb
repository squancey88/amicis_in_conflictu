<%= render Common::PageHeaderComponent.new(presenter: @presenter) %>

<%= render @army_list %>
<%= render Units::ListComponent.new(army_list: @army_list) %>
<%= render EquipmentAttachments::ListComponent.new(army_list: @army_list) %>
