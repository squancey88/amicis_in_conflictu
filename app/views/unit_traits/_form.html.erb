<%= form_with(model: unit_trait) do |form| %>

  <%= render RecordFormWrapperComponent.new(record: unit_trait, form:) do |component| %>

    <% component.with_form_box(title: "Details") do %>

      <%= form.hidden_field :game_system_id %>
      <%= form.text_field(:name) %>
      <%= form.check_box(:active) %>

      <%= render partial: "unit_trait_category_mappings/form_embed", locals: { record: unit_trait, form: } %>
    <% end %>

    <% component.with_form_box(title: "Description") do %>
      <%= render TextEditorComponent.new(form:, attribute: :rich_description) %>
    <% end %>
  <% end %>
<% end %>
